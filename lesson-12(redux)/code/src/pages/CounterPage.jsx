import Counter from "../components/Counter/Counter";

const CounterPage = ({ initialCounter }) => {
    
  return <Counter />; //(props) => <WrappedComponent {...props} {...stateProps} />
};

export default CounterPage;
